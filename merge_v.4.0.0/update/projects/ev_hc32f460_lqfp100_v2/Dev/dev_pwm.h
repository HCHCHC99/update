#ifndef DEV_PWM_H_#define DEV_PWM_H_#include "device_manager.h"#include "Pwm.h"#include <stdint.h>#include <stdbool.h>// ========== PWM设备命令码定义 ==========// 注意：CMD_BASE_PWM 已在 device_manager.h 中定义为 0x1000#define CMD_PWM_SET_DUTY            (CMD_BASE_PWM + 0x01)   // 立即设置占空比#define CMD_PWM_SET_DUTY_SLOW       (CMD_BASE_PWM + 0x02)   // 缓启动设置占空比#define CMD_PWM_SET_FREQ            (CMD_BASE_PWM + 0x03)   // 设置频率#define CMD_PWM_START               (CMD_BASE_PWM + 0x04)   // 启动PWM#define CMD_PWM_STOP                (CMD_BASE_PWM + 0x05)   // 停止PWM#define CMD_PWM_OUTPUT_ENABLE       (CMD_BASE_PWM + 0x06)   // 使能输出#define CMD_PWM_OUTPUT_DISABLE      (CMD_BASE_PWM + 0x07)   // 禁用输出#define CMD_PWM_EMERGENCY_STOP      (CMD_BASE_PWM + 0x08)   // 紧急停止// ========== PWM设备配置 ==========typedef struct {    CM_TMRA_TypeDef *tmr;       // TimerA单元    uint32_t periphClk;          // 外设时钟    uint32_t channel;            // PWM通道    uint8_t port;                // GPIO端口    uint16_t pin;                // GPIO引脚    uint16_t pinFunc;            // GPIO功能号    uint8_t countMode;           // 计数模式    uint8_t countDir;            // 计数方向    uint32_t defaultFreqHz;      // 默认频率    uint16_t defaultDutyPercent; // 默认占空比    pwm_active_t activePolarity; // 有效电平极性} PWM_Config_t;// ========== PWM设备结构体 ==========typedef struct {    pwm_t pwm_instance;          // PWM抽象层实例    PWM_Config_t config;         // 设备配置    uint8_t initialized;         // 初始化标志    uint8_t output_enabled;      // 输出使能标志    uint32_t last_update_time;   // 上次更新时间（用于缓启动）} PWM_Device_t;// ========== 命令参数结构体 ==========typedef struct {    uint16_t dutyPercent;        // 目标占空比(%)} PWM_DutyCmd_t;typedef struct {    uint16_t targetDuty;         // 目标占空比(%)    uint16_t rampTimeMs;         // 缓启动时间(ms)} PWM_RampCmd_t;typedef struct {    uint32_t freqHz;             // 目标频率(Hz)} PWM_FreqCmd_t;// ========== 标准设备操作接口 ==========DeviceResult_t PWM_Device_Init(void* handle);DeviceResult_t PWM_Device_Deinit(void* handle);DeviceResult_t PWM_Device_Read(void* handle, void* data, uint32_t size);DeviceResult_t PWM_Device_Write(void* handle, const void* data, uint32_t size);DeviceResult_t PWM_Device_Control(void* handle, DeviceCommandData_t* cmd);DeviceResult_t PWM_Device_Update(void* handle);// ========== PWM设备特定接口 ==========DeviceResult_t PWM_Device_SetDuty(PWM_Device_t* dev, uint16_t dutyPercent);DeviceResult_t PWM_Device_SetDutyWithRamp(PWM_Device_t* dev, uint16_t targetDuty, uint16_t rampTimeMs);DeviceResult_t PWM_Device_SetFrequency(PWM_Device_t* dev, uint32_t freqHz);DeviceResult_t PWM_Device_Start(PWM_Device_t* dev);DeviceResult_t PWM_Device_Stop(PWM_Device_t* dev);DeviceResult_t PWM_Device_EmergencyStop(PWM_Device_t* dev);DeviceResult_t PWM_Device_OutputEnable(PWM_Device_t* dev);DeviceResult_t PWM_Device_OutputDisable(PWM_Device_t* dev);// ========== 状态查询接口 ==========uint16_t PWM_Device_GetDuty(PWM_Device_t* dev);uint32_t PWM_Device_GetFrequency(PWM_Device_t* dev);pwm_state_t PWM_Device_GetState(PWM_Device_t* dev);bool PWM_Device_IsOutputEnabled(PWM_Device_t* dev);#endif /* DEV_PWM_H_ */