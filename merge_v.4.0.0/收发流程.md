# UDS OTA Flow (3-Phase Cross-Reset)

> **相关文档:** [flash分区.md](flash分区.md) — 扇区布局 | [flash操作.md](flash操作.md) — Flash 函数全集 | [OTA-flash补充.md](OTA-flash补充.md) — OTA 跳转参数 & 空闲扇区 | [CAN适配层修改.md](CAN适配层修改.md) — CAN 驱动架构与适配层

## Shared Flash State

- **Sector**: `0x00010000` (56 bytes, `stc_uds_shared_t`)
- **Magic**: `0x55445300` ("UDS\0")
- **Fields**: `magic | phase | target_slot | fw_size | fw_crc | result | pending_sid | reserved[7]`

### Phase Values

| Phase | Value | Who Writes | Meaning |
|-------|-------|------------|---------|
| `UDS_PHASE_IDLE` | 0 | Bootloader (clear) | Normal operation |
| `UDS_PHASE_ENTER_BOOTLOADER` | 1 | APP (0x31 handler) | Request to enter bootloader |
| `UDS_PHASE_PROGRAMMING_DONE` | 2 | Bootloader (FlashDownload) | Firmware download complete |

### pending_sid Values

| pending_sid | Meaning | Who Sets | Who Sends ACK |
|-------------|---------|----------|---------------|
| 0x11 | ECU Reset deferred ACK | Bootloader (0x11 handler) | APP (App_CheckPendingUdsAck) |
| 0x31 | RoutineControl deferred ACK | APP (Phase 1: `UdsShared_Write`) | Bootloader (Phase 2: `Bootloader_UdsMain`, 条件发送后清零) |
| 0 | No pending ACK | Bootloader (UdsShared_Clear) | - |

> Phase 1 写 Flash 时同时设置 `phase=ENTER_BOOTLOADER` 和 `pending_sid=0x31`，不再使用 `UdsShared_SetPhase`（该函数会清零 `pending_sid`），改为直接构造 `stc_uds_shared_t` 后调用 `UdsShared_Write`。

### Design Rule

> **Whoever receives the UDS command writes to shared Flash and resets WITHOUT sending a CAN response. The next firmware after reboot sends the deferred positive response.**

---

## OTA 状态机与 Flash 标志位

整个 OTA 流程是一个 **跨复位的状态机**。当前运行在哪个固件（APP/Bootloader）并不重要——状态机的"当前状态"持久化在 Flash 中，固件启动后读取 Flash 即可知道自己该做什么。

### 状态机图

```
                         ┌──────────────────────────────────────┐
                         │         RECOVERY (双APP故障)           │
                         │   Bootloader 直接进入 UDS 编程模式      │
                         │   phase=ENTER_BL, pending_sid=0        │
                         └────────────┬──────────────────────────┘
                                      │ RunBootloaderForever()
                                      │ → Bootloader_UdsMain()
                                      ▼
     ┌─────────────────────────────────────────────────────────────┐
     │                                                             │
     ▼                                                             │
┌─────────┐  0x31(APP)  ┌──────────────┐  下载完成    ┌──────────────┐
│  IDLE   │ ──────────→ │ ENTER_       │ ──────────→ │ PROGRAMMING_ │
│ (NORMAL)│             │ BOOTLOADER   │             │ DONE         │
│         │ ◄────────── │              │             │              │
│ phase=0 │  APP补发ACK │ phase=1      │             │ phase=2      │
│ magic=X │  后擦除Flash│ pending=0x31 │             │ result=1     │
└─────────┘             └──────────────┘             └──────┬───────┘
     ▲                                                      │
     │                                                      │ 0x11(BL)
     │                                   ┌──────────────┐   │
     │                                   │  WAIT_RESET  │ ◄─┘
     │                                   │              │
     │                                   │ phase=2      │
     │                                   │ pending=0x11 │
     │                                   └──────┬───────┘
     │                                          │ NVIC_SystemReset()
     │                                          │
     └──────────────────────────────────────────┘
                     APP 启动, 读 pending_sid=0x11
                     发 51 01 ACK, UdsShared_Clear()
```

> **核心思想**：状态不保存在 RAM 变量中，而是以 `phase` + `pending_sid` 的组合持久化在 `0x10000` 扇区。每次复位后，固件通过读取这两个字段决定自己处于状态机的哪个阶段。

### 状态定义

| 状态 | phase | pending_sid | magic | 谁写入 | 触发条件 |
|------|:-----:|:-----------:|:-----:|--------|---------|
| **IDLE** | 0 或 X | 0 | 非 `UDS_SHARED_MAGIC` | Phase 3 清除 | OTA 完成 / 从未 OTA |
| **ENTER_BOOTLOADER** | 1 | `0x31` | `0x55445300` | Phase 1 (APP) | APP 收到 0x31 |
| **PROGRAMMING_DONE** | 2 | 0 | `0x55445300` | Phase 2 (BL) | 下载完成 |
| **WAIT_RESET** | 2 | `0x11` | `0x55445300` | Phase 2 (BL) | 收到 0x11 |
| **RECOVERY** | — | 0 | — | (无 Phase 1) | 双 APP 均 WDT≥3 |

### Flash 区域总览

| 区域 | 地址 | 大小 | 关键字段 |
|------|------|------|---------|
| UDS Shared State | `0x00010000` | 56B | `magic`(4B) + `phase`(4B) + `target_slot`(4B) + `fw_size`(4B) + `fw_crc`(4B) + `result`(4B) + **`pending_sid`**(4B) + `reserved[7]` |
| APP Run Slot | `0x0007C000` | 4B | `SLOT_A_MAGIC`(`0x5A5A5A5A`)→APP1 / `SLOT_B_MAGIC`(`0xA5A5A5A5`)→APP2 |
| APP1 State | `0x00016000` | 8B | `WDT_FEED_CONTROL_APP1` + `WDT_COUNT_APP1` |
| APP2 State | `0x00018000` | 8B | `WDT_FEED_CONTROL_APP2` + `WDT_COUNT_APP2` |

---

### 状态转换详解

#### 转换 ①: IDLE → ENTER_BOOTLOADER（Phase 1）

| | 详情 |
|---|------|
| **运行固件** | APP (APP1 或 APP2) |
| **触发** | TBOX 发送 0x31 (RoutineControl)，APP 上下文 |
| **代码** | `uds_diagnostic.c:uds_handle_routine_control()`，`SCB->VTOR == APP1/APP2` 分支 |
| **Flash 写入** | `0x10000`: `magic=0x55445300`, `phase=1`, `pending_sid=0x31`，其余清零 |
| **CAN 响应** | ❌ 不发 |
| **结束动作** | 设置 `g_delayed_reset_ms` → `NVIC_SystemReset()` |
| **PB6** | 闪 3 次 (100ms) |

#### 转换 ②: ENTER_BOOTLOADER → PROGRAMMING_DONE（Phase 2 前半）

| | 详情 |
|---|------|
| **运行固件** | Bootloader |
| **触发** | `Boot_StartupSequence()` 读到 `phase==1` → 进入 `Bootloader_UdsMain()` |
| **Flash 读取** | `pending_sid`：`==0x31` → 补发 `71 01 FF 00` 后清零；`!=0x31` → 跳过 |
| **Flash 写入** | `0x7C000`: `SLOT_A_MAGIC`（`Boot_SetRunSlotToAddr(APP1)`） |
| **UDS 下载** | TBOX: `10 02` → `27` → `34` → `36×N` → `37` |
| **下载完成** | 检测到 `FW_UPDATE_COMPLETE` → 写 `0x10000`: `phase=2`, `result=1`, `target_slot=APP2` |
| | **同时**: `ClearAppStateBySlot(SLOT_APP2)` → 写 `0x18008`: `WDT_COUNT_APP2=0` |
| **PB6** | 闪 3 次 (200ms)，在函数入口处 |

#### 转换 ③: PROGRAMMING_DONE → WAIT_RESET（Phase 2 后半）

| | 详情 |
|---|------|
| **运行固件** | Bootloader（仍在 `Bootloader_UdsMain()` 的 while(1) 中） |
| **触发** | TBOX 发送 0x11 (ECU Reset) |
| **代码** | `uds_diagnostic.c:uds_handle_ecu_reset()`，`SCB->VTOR==0` 分支 |
| **Flash 读取+写入** | 读 `0x10000` 校验 magic → 写 `pending_sid=0x11`，phase 保持 2 |
| **CAN 响应** | ❌ 不发 |
| **结束动作** | 设置 `g_delayed_reset_ms` → `NVIC_SystemReset()` |

#### 转换 ④: WAIT_RESET → IDLE（Phase 3）

| | 详情 |
|---|------|
| **运行固件** | Bootloader 重启 → 读 `phase==2`(不是1) → 跳过 UDS → `JumpToApp(APP1)` → APP1 启动 |
| **触发** | APP1 启动后调用 `App_CheckPendingUdsAck()` |
| **Flash 读取** | `pending_sid==0x11` → 通过 raw CAN 发送 `04 51 01 00 00 00 00 00` |
| **Flash 写入** | `UdsShared_Clear()` → 擦除整个 `0x10000` 扇区（magic 归零） |
| **CAN 响应** | ✅ 发 51 01 ACK（TBOX 收到后 OTA 流程正式结束） |
| **PB6** | 闪 3 次 (300ms) |

#### 转换 ⑤: IDLE → RECOVERY → PROGRAMMING_DONE（双 APP 故障救援）

| | 详情 |
|---|------|
| **运行固件** | Bootloader |
| **触发** | `SelectTargetSlot()` 返回 `SLOT_NONE`（APP1、APP2 均 WDT≥3） |
| **代码** | `RunBootloaderForever()` → `UdsOta_Bootloader_Enter()` → `Bootloader_UdsMain()` |
| **与正常流程的区别** | 没有 Phase 1 → `pending_sid==0` → Phase 2 跳过 31 ACK |
| **后续流程** | 与转换 ②③④ 相同：下载 → `FW_UPDATE_COMPLETE` → `ClearAppStateBySlot` → 0x11 → 复位 → 跳转 APP2 |
| **PB6** | `ShowBootStatus()` 闪 5 次，然后 Phase 2 闪 3 次 |

> 此路径依赖 `g_u32Debug_ClearAppState = 0`。若该值为 3，每次上电 WDT 计数归零，双槽位故障切换被绕过。

### 复位时的状态分发

`Boot_StartupSequence()` 每次上电执行以下判断，这就是状态机的"消费端"：

```
读 0x10000:
  magic != UDS_SHARED_MAGIC  ──→ IDLE: 正常启动 APP
  magic == UDS_SHARED_MAGIC:
    phase == 1 (ENTER_BOOTLOADER) ──→ 状态 ENTER_BOOTLOADER:
        Bootloader_UdsMain()
          → 读 pending_sid: 0x31→发ACK, 0→跳过
          → UDS下载 → PROGRAMMING_DONE → 等0x11 → 复位
    
    phase == 2 (PROGRAMMING_DONE) ──→ 状态 PROGRAMMING_DONE/WAIT_RESET:
        跳过 UDS → 正常启动 APP
        → APP 启动后 App_CheckPendingUdsAck()
          → pending_sid: 0x11→发51 01, 0→啥也不做
```

### 状态转换汇总表

| 转换 | 从 → 到 | 写 Flash 者 | 写了什么 | 复位 |
|------|---------|-----------|---------|:--:|
| ① | IDLE → ENTER_BOOTLOADER | APP | `phase=1, pending_sid=0x31, magic=VALID` | ✓ |
| ② | ENTER_BOOTLOADER → PROGRAMMING_DONE | Bootloader | `phase=2, result=1` + 清零 APP2 WDT | ✗ |
| ③ | PROGRAMMING_DONE → WAIT_RESET | Bootloader | `pending_sid=0x11` | ✓ |
| ④ | WAIT_RESET → IDLE | APP1 | 发 51 01 → 擦除扇区 (magic=0) | ✗ |
| ⑤ | IDLE → RECOVERY | (无，跳过 Phase 1) | 双 APP 故障 → 直接进入 UDS 模式 | ✗ |---

## Phase 1: APP (Routine Control 0x31 triggers transition)

```
TBOX                          ECU (APP)
 |                              |
 |-- 10 03 (extended session) -->|
 |<-- 50 03 --------------------|
 |                              |
 |-- 27 01 (seed request) ----->|
 |<-- 67 01 [4-byte seed] ------|
 |                              |
 |-- 27 02 [4-byte key] ------->|
 |<-- 67 02 --------------------|  (security unlocked)
 |                              |
 |-- 31 01 xx xx [routine ctrl] ->|
 |                              |
 |   [APP detects SCB->VTOR is APP1/APP2]
 |   Write UDS shared Flash:
 |     magic = UDS_SHARED_MAGIC
 |     phase = UDS_PHASE_ENTER_BOOTLOADER
 |     pending_sid = 0x31
 |   *resp_len = 0 (NO CAN response!)
 |   NVIC_SystemReset()
 |                              |
 |   ---- ECU RESETS ----       |
```

### Step detail

| Step | Who | Action |
|------|-----|--------|
| 1.1 | TBOX | Send `10 03` (extended diagnostic session) |
| 1.2 | APP | Respond `50 03` (positive) |
| 1.3 | TBOX | Send `27 01` (request seed) |
| 1.4 | APP | Respond `67 01` + 4-byte seed |
| 1.5 | TBOX | Send `27 02` + 4-byte key (CRC8) |
| 1.6 | APP | Respond `67 02` (security unlocked) |
| 1.7 | TBOX | Send `31 01 xx xx` (routine control, RID=...) |
| 1.8 | APP | Detect `SCB->VTOR` is APP, write shared Flash: `phase=ENTER_BOOTLOADER`, `pending_sid=0x31`, `magic=UDS_SHARED_MAGIC` |
| 1.9 | APP | Set `*resp_len = 0` (NO CAN response), call `NVIC_SystemReset()` |

---

## Phase 2: Bootloader (Download + 0x34/0x36/0x37 + deferred 31 ACK)

```
                         ECU (Bootloader)
                              |
   Bootloader_UdsMain() starts:
   1. Read UDS shared Flash, check pending_sid:
      if pending_sid == 0x31 → send deferred 31 ACK (71 01 FF 00), clear pending_sid
      if pending_sid != 0x31 → skip (e.g. recovery mode, no Phase 1)
   2. Init FlashDownload (target = UDS_TARGET_FLASH_ADDR = APP2, 0x4C000)
   3. Init UDS
   4. Handle 0x31 in bootloader context:
      Boot_SetRunSlotToAddr(UDS_POST_FLASH_BOOT_ADDR) --> writes APP1 magic to 0x7C000
                              |
   5. Enter main loop:
      CAN poll + ISOTP/UDS + FlashDownload + WDT (500ms)
                              |
 TBOX                          |
 |                              |
 |-- 10 02 (programming session) ->|
 |<-- 50 02 --------------------|
 |                              |
 |-- 27 01 (seed request) ----->|
 |<-- 67 01 [4-byte seed] ------|
 |                              |
 |-- 27 02 [4-byte key] ------->|
 |<-- 67 02 --------------------|  (security unlocked)
 |                              |
 |-- 34 00 44 [addr=0x08004000] [size] ->|   (RequestDownload)
 |   Bootloader maps: addr - 0x08004000 + FW_APP_START_ADDR
 |                   = 0x0004C000 (APP2)
 |   Erase APP2 sectors
 |<-- 74 40 00 -----------------|  (max block size = 0x4000)
 |                              |
 |-- 36 01 [data block 1] ----->|
 |<-- 76 01 --------------------|  (transfer data ACK)
 |                              |
 |-- 36 02 [data block 2] ----->|
 |<-- 76 02 --------------------|
 |   ... (more 0x36 blocks) ... |
 |                              |
 |-- 36 NN [last data block] -->|
 |<-- 76 NN --------------------|
 |                              |
 |-- 37 (request transfer exit) ->|
 |   FlashDownload_Task() does verify
 |<-- 77 -----------------------|  (transfer exit ACK)
 |                              |
 |   In main loop, detect:
 |   FlashDownload_GetState() == FW_UPDATE_COMPLETE
 |   Write UDS shared Flash:
 |     phase = PROGRAMMING_DONE
 |     result = 1 (success)
 |     target_slot = SLOT_APP2
 |   Does NOT reset - keeps running
 |                              |
 |-- 11 01 (ECU Reset) ------->|
 |                              |
 |   [Bootloader detects SCB->VTOR == 0, enters uds_handle_ecu_reset]
 |   MAIN_D(">>> Handle 0x11 (ECU Reset)...")
 |   MAIN_D("  Bootloader context, writing pending_sid=0x11 to flash")
 |   Read UDS shared flash:
 |     magic=0x55445300, phase=PROGRAMMING_DONE, pending_sid=0
 |   Write: pending_sid = 0x11
 |   MAIN_D("  pending_sid=0x11 written to flash at 0x00010000")
 |   *resp_len = 0 (NO CAN response!)
 |   MAIN_D("  Resetting now...")
 |   NVIC_SystemReset()
 |                              |
 |   ---- ECU RESETS ----       |
```

### Step detail

| Step | Who | Action |
|------|-----|--------|
| 2.1 | Bootloader | `Bootloader_UdsMain()` starts, reads shared Flash: if `pending_sid==0x31` send `71 01 FF 00` (31 ACK) and clear `pending_sid`; otherwise skip |
| 2.2 | Bootloader | Init FlashDownload target = APP2 (0x4C000, max 80KB) |
| 2.3 | Bootloader | Init UDS, handle 0x31: call `Boot_SetRunSlotToAddr(UDS_POST_FLASH_BOOT_ADDR)` → write APP1 magic to 0x7C000 |
| 2.4 | TBOX | Send `10 02` (programming session) |
| 2.5 | Bootloader | Respond `50 02` |
| 2.6 | TBOX | Send `27 01` → `27 02` (security unlock) |
| 2.7 | TBOX | Send `34 00 44 [addr=0x08004000] [size]` (RequestDownload) |
| 2.8 | Bootloader | Map `0x08004000` → `0x0004C000` (APP2), erase sectors, respond `74 40 00` |
| 2.9 | TBOX | Send `36 01 [data]` … `36 NN` (TransferData blocks) |
| 2.10 | Bootloader | Respond `76 01` … `76 NN` (block ACK) |
| 2.11 | TBOX | Send `37` (RequestTransferExit) |
| 2.12 | Bootloader | Respond `77`, trigger verify |
| 2.13 | Bootloader | Main loop detects `FW_UPDATE_COMPLETE`, writes shared Flash: `phase=PROGRAMMING_DONE, result=1, target_slot=SLOT_APP2` |
| 2.14 | TBOX | Send `11 01` (ECU Reset) |
| 2.15 | Bootloader | `uds_handle_ecu_reset`: `MAIN_D` prints, writes `pending_sid=0x11`, `*resp_len=0`, `NVIC_SystemReset()` |

---

## Phase 3: Bootloader → APP1 → deferred 51 01 ACK

```
                         ECU (Bootloader restarts)
                              |
   Boot_StartupSequence():
   1. Check shared RAM debug flag
   2. Check UDS Shared State:
      phase=PROGRAMMING_DONE (NOT ENTER_BOOTLOADER => skip Bootloader_UdsMain)
   3. CheckAndClearAppState()
   4. GetWdtResetType() -- NOT a WDT reset
   5. GetCurrentSlot() -- reads APP_RUN_SLOT_ADDR (0x7C000)
      → sees SLOT_A_MAGIC (0x5A5A5A5A) → eCurrentSlot = APP1
   6. InitAppInfo for APP1 and APP2
   7. ValidateSlotFlag, UpdateAppState, HandleWatchdogReset
   8. SelectTargetSlot → APP1 (current slot, healthy)
   9. UpdateSlotFlagToFlash → write APP1 magic to 0x7C000
  10. Bootloader_JumpToApp(APP1_START_ADDR = 0x1A000)
      → set MSP, VTOR=0x1A000, branch to reset vector
                              |
                         ECU (APP1 starts)
                              |
   APP1 main():
   1. SCB->VTOR = APP1_START_ADDR (0x1A000)
   2. __enable_irq()
   3. Hardware_Init()  (内含 CanIf_Init: CAN硬件就绪)
   4. MAIN_D("APP1 Running...")
   5. App_CheckPendingUdsAck():
      - Read UDS shared flash (0x10000)
      - magic = 0x55445300 ✓
      - pending_sid = 0x11 → YES!
      - MAIN_D("Sending pending 11 01 ACK (51 01)")
      - Build CAN frame:
        CAN ID = 0x18DAF103 (phys response)
        DLC = 8
        Data = {0x04, 0x51, 0x01, 0x00, 0x00, 0x00, 0x00, 0x00}
              ^^^^  ^^^^^^
              PCI   UDS positive response to 0x11
        (ISOTP single frame, PCI=0x04 = 4 bytes payload)
      - CanIf_Send(&stcMsg) --> raw CAN send (ISOTP NOT initialized yet)
      - UdsShared_Clear() → erase shared sector
                              |
 TBOX                          |
 |<-- [ISOTP SF] 51 01 --------|  (deferred ECU reset ACK!)
 |                              |
 |   [OTA flow complete]        |
```

### Step detail

| Step | Who | Action |
|------|-----|--------|
| 3.1 | Bootloader | Restart after 0x11 reset, `Boot_StartupSequence()` checks UDS shared: phase is PROGRAMMING_DONE (not ENTER_BOOTLOADER), skips UDS mode |
| 3.2 | Bootloader | Read `APP_RUN_SLOT_ADDR` (0x7C000) → sees APP1 magic → select APP1 as target |
| 3.3 | Bootloader | `Bootloader_JumpToApp(0x1A000)`: set MSP from APP1 vector table, set VTOR=0x1A000, branch to reset vector |
| 3.4 | APP1 | `Hardware_Init()` (内含 `CanIf_Init`: GPIO/波特率/中断, CAN 硬件就绪), then `App_CheckPendingUdsAck()` |
| 3.5 | APP1 | Read shared Flash: `pending_sid == 0x11` → send deferred `51 01` via raw CAN (manual ISOTP SF PCI byte `0x04`) |
| 3.6 | APP1 | `UdsShared_Clear()` → erase shared sector, enter normal main loop |

---

## Context Detection

```c
// In UDS handlers (uds_diagnostic.c):
if (SCB->VTOR == APP1_START_ADDR || SCB->VTOR == APP2_START_ADDR) {
    // Running in APP context
} else {
    // Running in Bootloader context (SCB->VTOR == 0 for bootloader)
}
```

## Address Mapping

```
TBOX address:  0x08004000
MAP_TBOX_ADDR_TO_FLASH(0x08004000)
  = 0x08004000 - 0x08004000 + 0x0004C000
  = 0x0004C000  (APP2_START_ADDR)
```

## Key Macros

| Macro | Value | Purpose |
|-------|-------|---------|
| `UDS_TARGET_FLASH_ADDR` | `0x4C000` (APP2) | Flash download target |
| `UDS_POST_FLASH_BOOT_ADDR` | `0x1A000` (APP1) | Forced boot slot after flash (macro, currently hardcoded) |
| `FW_APP_START_ADDR` | `0x0004C000` (APP2) | Base for TBOX address mapping |
| `FW_APP_MAX_SIZE` | `0x00014000` (80KB) | APP2 下载窗口/固件大小上限（2026-08-02 由 48KB 调整） |
| `TBOX_ADDR_END` | `0x08018000` | TBOX 地址窗终点（= START + 80KB，2026-08-02 调整） |
| `UDS_SHARED_SECTOR_BASE` | `0x00010000` | Shared Flash state sector |
| `UDS_SHARED_MAGIC` | `0x55445300` | "UDS\0" validation magic |
| `APP_RUN_SLOT_ADDR` | `0x0007C000` | Active slot magic storage |

> 各宏对应 Flash 操作的详细函数、扇区、保护状态见 [flash操作.md](flash操作.md)

## Dual-Slot Failure Recovery

当 APP1 和 APP2 均因 WDT 复位 ≥3 次被标记为 `DISABLED` 时：

1. `Boot_StartupSequence()` → `SelectTargetSlot()` 返回 `SLOT_NONE`
2. `RunBootloaderForever()` → 直接调用 `UdsOta_Bootloader_Enter()` 进入 **Phase 2**（UDS 编程模式）
3. 由于没有 Phase 1，`pending_sid != 0x31`，Phase 2 **不会发送**多余的 31 ACK
4. ECU 等待 TBOX 通过 UDS 下载新固件来恢复
5. 下载完成（`FW_UPDATE_COMPLETE`）后，自动调用 `ClearAppStateBySlot(SLOT_APP2)` 清零 APP2 的 WDT 故障计数
6. TBOX 发 0x11 复位后，Bootloader 检测到 APP2 已恢复为 `AVAILABLE`，正常跳转

> 此机制依赖 `g_u32Debug_ClearAppState = 0`（不自动清零 WDT 计数），否则每次上电计数归零，双槽位故障切换被绕过。

## PB6 Phase Indicator（已删除 2026-08-02）

原设计用 PB6 GPIO 输出指示 OTA 三阶段（Phase 1/2/3 各闪 3 次）。**2026-08-02 已全部移除**：
- 四驱工程中 PB6 是电机2下管 PWM 引脚（`pwm_hw.c` CH_M2_LV），Phase 1 的闪烁会重新配置该引脚并手动翻转，导致过流故障、主循环被卡住、延迟复位无法执行；
- 已删除：四驱 `uds_diagnostic.c` 的 Phase 1 闪烁；OTA boot/app1/app2 的 Phase 1/2/3 闪烁；boot 的 `ShowBootStatus()` / `GetBootStatus()`。

## 修改记录 (2026-07-12)

| 修改项 | 涉及文件 | 说明 |
|--------|---------|------|
| PB6 相位指示 | `uds_diagnostic.c`, `Bootloader_App.c`, `main.c` (boot/app1/app2) | 删除 main.c 阻塞闪烁，改为 Phase 1/2/3 各闪 3 次 |
| `pending_sid=0x31` | `uds_diagnostic.c` (boot/app1/app2) | Phase 1 直接写 Flash（含 pending_sid），不再用 UdsShared_SetPhase |
| 条件 31 ACK | `Bootloader_App.c` (boot/app1/app2) | Phase 2 读 pending_sid，仅 `==0x31` 时才发 ACK |
| 双 APP 故障恢复 | `Bootloader_App.c` (boot/app1/app2) | `RunBootloaderForever()` → `UdsOta_Bootloader_Enter()` |
| 下载后清零 WDT | `Bootloader_App.c` (boot/app1/app2) | `FW_UPDATE_COMPLETE` 后 `ClearAppStateBySlot(SLOT_APP2)` |
| WDT 计数持久化 | `Bootloader_App.c` (boot/app1/app2) | `g_u32Debug_ClearAppState` 从 3 改为 0 |
| app2 UDS 对齐 | `uds_ota.h/c` (新建), `main.c`, `Bootloader_App.c` (app2) | app2 的 UDS 封装层、main.c 精简与 app1 一致 |

### 修改记录 (2026-08-02)

| 修改项 | 涉及文件 | 说明 |
|--------|---------|------|
| APP1/APP2 分区 80KB | `Bootloader_App.c`, `flash_download.h/c` (boot/app1/app2) | `max_firmware_size` 48→80KB、`user_end_addr` +0xC000→+0x14000、`FW_APP_MAX_SIZE` 0xC000→0x14000、`TBOX_ADDR_END` 0x08010000→0x08018000 |
| PB6 相位指示删除 | `uds_diagnostic.c`, `Bootloader_App.c` (boot/app1/app2 + 四驱工程) | 三阶段闪烁及 `ShowBootStatus()` 全部移除（四驱 PB6 为电机引脚，会触发故障） |
| app1 31 ACK 发送方式统一 | `Bootloader_App.c` (app1) | raw CAN 改回 `isotp_send_message`，与 boot 一致 |
| 四驱工程移植并实测通过 | 四驱工程 (D:\260706_NL) | Phase 1 → OTA boot Phase 2 → Phase 3 全链路验证通过；详见四驱 `app/can/uds/uds移植.md` |

## 0x11 Handler Implementation Details

The 0x11 handler (`uds_handle_ecu_reset`) in `uds_diagnostic.c` uses `MAIN_D()` for all debug prints (instead of `UDS_I()`) so output is always visible regardless of `UDS_DEBUG`:

```c
static void uds_handle_ecu_reset(uint8_t* data, uint8_t len, uint8_t* resp, uint8_t* resp_len)
{
    MAIN_D(">>> Handle 0x11 (ECU Reset), VTOR=0x%08X\r\n", (unsigned int)SCB->VTOR);
    // ...
    if (SCB->VTOR == 0) {
        MAIN_D("  ECU Reset: Bootloader context, writing pending_sid=0x11 to flash\r\n");
        stc_uds_shared_t state;
        UdsShared_Read(&state);
        MAIN_D("  Shared state: magic=0x%08X, phase=%d, pending_sid=0x%02X\r\n", ...);
        if (state.magic == UDS_SHARED_MAGIC) {
            state.pending_sid = 0x11;
            UdsShared_Write(&state);
            MAIN_D("  pending_sid=0x11 written to flash at 0x%08X\r\n", ...);
        }
        *resp_len = 0;
        MAIN_D("  Resetting now...\r\n");
        NVIC_SystemReset();
        while(1);
    } else {
        MAIN_D("  ECU Reset: APP context, sending normal response\r\n");
        resp[0] = reset_type;
        *resp_len = 1;
    }
}
```

## Deferred 51 01 ACK Implementation Details

`App_CheckPendingUdsAck()` sends the deferred ACK using **raw CAN** (not ISOTP) because ISOTP is not yet initialized when this function runs in APP startup:

```c
if (state.pending_sid == 0x11) {
    MAIN_D("  Sending pending 11 01 ACK (51 01)\r\n");
    /* ISOTP single frame: PCI=0x04 (4 bytes payload) + 51 01 00 00, DLC=8 */
    CanMsg_t stcMsg;
    uint8_t au8Data[8] = {0x04, 0x51, 0x01, 0x00, 0x00, 0x00, 0x00, 0x00};
    // ... fill stcMsg ...
    CanIf_Send(&stcMsg);
}
```

## Post-OTA Boot Address Control

After OTA flash completes, the bootloader always boots APP1. This is controlled by:

```c
// Bootloader_App.h, line 31:
#define UDS_POST_FLASH_BOOT_ADDR         APP1_START_ADDR   // = 0x1A000

// uds_diagnostic.c, 0x31 handler (bootloader context):
Boot_SetRunSlotToAddr(UDS_POST_FLASH_BOOT_ADDR);  // writes APP1 magic to 0x7C000
```

To change the post-OTA boot target (e.g., to APP2), change `UDS_POST_FLASH_BOOT_ADDR` to `APP2_START_ADDR` in all three copies of `Bootloader_App.h` (boot/, app1/, app2/).

Note: this is a development hardcode. The plan is to make it dynamic based on which slot received the new firmware.
